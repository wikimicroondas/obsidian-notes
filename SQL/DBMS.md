## Online Transaction Processing (OLTP)

It is used to `update` and `follow` a register in a dynamic way, it is conceived to be accessed and modified diary.
> Applications of diary use.
> Users: thousands

## Online Analytical Processing (OLAP)

It is used to `take business decisions`; order data, manage analytics
> Saves more data than OLTP
> Usuarios: hundreds

